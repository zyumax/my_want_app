
<h1 class="display-4 text-primary">マイリスト</h1>

<div class="sum-price"> 
  <label>合計金額：</label>
  <%= number_to_currency(@total_price, unit: "¥", precision: 0) %>
</div>

<p><%= link_to '+ 新規', new_product_path, class: "btn btn-primary" %></p>

<div class="container">
  <div class="row">
    <div>
      <% @products.each do |product| %>
        <% if product.evaluation == 5 %>
          <div class="card-body">
            <h3 class="card-title"><%= product.name %></h3>
            <h3 class="card-subtitle"><%= number_to_currency(product.price, unit: "¥", precision: 0) %></h3>
            <h4 class = "star-eva">"★★★★★"</h4>
            <p><%= link_to '編集', edit_product_path(id: product.id, sugie: "sugie"), method: :get, local: true %></p>
            <p><%= link_to '- 削除', product_path(product.id), method: :delete, local: true %></p>
          </div>
        <% end %>
      <% end %>
    </div>
    <div>
      <% @products.each do |product| %>
        <% if product.evaluation == 4 %>
          <div class="card-body">
            <h3 class="card-title"><%= product.name %></h3>
            <h3 class="card-subtitle"><%= number_to_currency(product.price, unit: "¥", precision: 0) %></h3>
            <h4 class = "star-eva">"★★★★☆"</h4>
            <p><%= link_to '編集', edit_product_path(id: product.id, sugie: "sugie"), method: :get, local: true %></p>
            <p><%= link_to '- 削除', product_path(product.id), method: :delete, local: true %></p>
          </div>
        <% end %>
      <% end %>
    </div>
    <div>
      <% @products.each do |product| %>
        <% if product.evaluation == 3 %>
          <div class="card-body">
            <h3 class="card-title"><%= product.name %></h3>
            <h3 class="card-subtitle"><%= number_to_currency(product.price, unit: "¥", precision: 0) %></h3>
            <h4 class = "star-eva">"★★★☆☆"</h4>
            <p><%= link_to '編集', edit_product_path(id: product.id, sugie: "sugie"), method: :get, local: true %></p>
            <p><%= link_to '- 削除', product_path(product.id), method: :delete, local: true %></p>
          </div>
        <% end %>
      <% end %>
    </div>
    <div>
      <% @products.each do |product| %>
        <% if product.evaluation == 2 %>
          <div class="card-body">
            <h3 class="card-title"><%= product.name %></h3>
            <h3 class="card-subtitle"><%= number_to_currency(product.price, unit: "¥", precision: 0) %></h3>
            <h4 class = "star-eva">"★★☆☆☆"</h4>
            <p><%= link_to '編集', edit_product_path(id: product.id, sugie: "sugie"), method: :get, local: true %></p>
            <p><%= link_to '- 削除', product_path(product.id), method: :delete, local: true %></p>
          </div>
        <% end %>
      <% end %>
    </div>
    <div>
      <% @products.each do |product| %>
        <% if product.evaluation == 1 %>
          <div class="card-body">
            <h3 class="card-title"><%= product.name %></h3>
            <h3 class="card-subtitle"><%= number_to_currency(product.price, unit: "¥", precision: 0) %></h3>
            <h4 class = "star-eva">"★☆☆☆☆"</h4>
            <p><%= link_to '編集', edit_product_path(id: product.id, sugie: "sugie"), method: :get, local: true %></p>
            <p><%= link_to '- 削除', product_path(product.id), method: :delete, local: true %></p>
          </div>
        <% end %>
      <% end %>
    </div>
  </div>
</div>

<p><%= link_to '予測画面(仮)', products_predict_path, class: "btn btn-success" %></p> 

<% if user_signed_in? %>
  <p><%= link_to 'サインアウト', destroy_user_session_path, method: :delete %></p>
<% end %>